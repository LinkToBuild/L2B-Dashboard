import { OverviewFloorplanSkeleton } from "@/shared/components/skeletons";

/** Generic module transition — overview floorplan as safe default */
export default function DashboardLoading() {
  return <OverviewFloorplanSkeleton />;
}
