import { l2bColors } from "./colors";

export const themeExtensions = {
  colors: l2bColors,
  fontFamily: {
    sans: ["var(--font-poppins)", "sans-serif"],
  },
  boxShadow: {
    // This matches the shadow from your design
    'l2b-soft': '0 0 15px 0 rgba(0, 0, 0, 0.15)',
  },
};